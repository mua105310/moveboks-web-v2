import { ProductConstraintModel } from "@/internal/models/package";

export interface PackageCardProps {
  product: ProductConstraintModel;
}

export default function PackageCard({ product }: PackageCardProps) {
  return (
    <div>
        
    </div>
  );
}
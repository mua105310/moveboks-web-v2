import { ProductConstraintModel } from "@/internal/package";

export default function ItemCard({ item }: { item: ProductConstraintModel }) {
  return (
    <div>
        {/* Image */}
        <div className="relative w-full h-1/2">
            
        </div>
        {/* Content */}
        <div>

        </div>
    </div>
  );
}